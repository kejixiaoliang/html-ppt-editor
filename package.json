{
  "name": "html-studio-visual-editor",
  "version": "1.0.0",
  "private": true,
  "description": "A local visual editor for importing, adjusting, previewing, and exporting single-file HTML pages.",
  "scripts": {
    "start": "node server/index.js",
    "check": "node --check app.js && node --check server/index.js && node scripts/check.js",
    "test": "npm run check"
  }
}

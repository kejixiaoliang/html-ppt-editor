# 第二轮优化计划：HTML PPT 编辑体验增强

## 1. 优化目标

当前 MVP 已经完成核心闭环：

> 导入 HTML → 预览 → 点击元素 → 修改属性 → 同步源码 → 导出 HTML

第二轮优化的目标是让产品更贴近 **HTML PPT 可视化编辑器**，而不是普通 HTML 源码预览工具。

重点方向：

- 弱化源码区域，让预览和属性编辑成为主工作区
- 强化 PPT 画布感，让中间区域更像 slide 编辑器
- 优化属性面板结构，降低编辑压力
- 增强选中元素反馈，让用户知道自己正在编辑什么

## 2. 本轮范围

本轮仍然保持轻量实现，不引入 React、Monaco 或复杂构建系统，继续基于当前静态 MVP 演进。

### 2.1 做什么

- 将源码区域改为可折叠面板
- 默认以预览画布为主
- 增加预览缩放控制
- 增加 PPT 画布尺寸选择
- 增加画布居中和外层舞台效果
- 属性面板增加分组
- 增加选中元素摘要
- 增加元素路径展示
- 增加元素尺寸展示
- hover 和 selected 状态增加浮层标签

### 2.2 暂不做什么

- 不做自由拖拽移动
- 不做边角 resize
- 不做 slide 缩略图列表
- 不做撤销/重做
- 不引入 Monaco Editor
- 不做多文件项目管理

这些内容放到后续第三轮继续推进。

## 3. 布局优化

### 3.1 当前问题

当前界面是三栏：

```text
源码区 | 预览区 | 属性区
```

这对开发者友好，但对 HTML PPT 编辑场景来说，源码区域存在感太强。

### 3.2 优化方案

改为：

```text
顶部工具栏
主工作区：预览画布 + 属性面板
底部/左侧折叠源码抽屉
```

第一步可以先做成：

- 源码区仍然保留
- 但宽度压缩
- 支持一键隐藏/显示
- 隐藏后预览区自动占据更多空间

## 4. 预览区优化

### 4.1 目标

预览区应该更像 PPT 画布，而不是普通网页 iframe。

### 4.2 功能

- 增加画布尺寸：
  - 16:9
  - 4:3
  - 自适应页面
- 增加缩放：
  - Fit
  - 50%
  - 75%
  - 100%
  - 125%
- 预览画布居中显示
- iframe 外层增加阴影和边界
- 舞台背景使用低干扰纹理
- 顶部显示当前画布模式

## 5. 属性面板优化

### 5.1 当前问题

当前属性面板字段较平铺，用户不容易判断哪些是内容，哪些是样式。

### 5.2 优化方案

分成以下区域：

- 当前选中
  - 标签名
  - 元素路径
  - 元素尺寸
  - editor id
- 内容
  - 文本
  - src
  - href
- 标识
  - id
  - class
- 字体
  - color
  - font-size
  - font-weight
- 外观
  - background-color
  - border-radius
  - box-shadow
  - opacity
- 尺寸与间距
  - width
  - height
  - margin
  - padding
- 变换
  - transform

## 6. 选中元素体验优化

### 6.1 目标

用户点击一个元素后，应清楚知道：

- 当前选中的是什么标签
- 它在 DOM 中的位置
- 它的视觉尺寸
- hover 到的元素是什么

### 6.2 功能

- hover 元素显示橙色边框
- hover 元素旁显示标签，例如 `h1.title`
- selected 元素显示强调边框
- selected 元素旁显示标签
- 属性面板显示 DOM 路径
- 属性面板显示尺寸信息

## 7. 验收标准

完成后应满足：

1. 打开页面后，预览区域是视觉中心
2. 源码区域可以隐藏和显示
3. 预览可以在不同缩放比例下查看
4. 可以切换常用 PPT 画布比例
5. 选中元素后，右侧显示元素路径和尺寸
6. 属性字段分组更清晰
7. 修改文本和样式仍然能同步源码
8. 导出的 HTML 不包含编辑器内部标识

## 8. 后续建议

第二轮完成后，第三轮可以继续做：

- 撤销/重做
- slide 缩略图管理
- 图片上传替换
- 键盘方向键微调
- 拖拽移动元素
- AI 针对选中元素局部编辑
